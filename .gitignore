# dependencies and build output
node_modules/
dist/

# local secrets — .env.example is the committed template
.env
.env.local
.env.*.local

# editor and OS noise
.history/
.DS_Store
Thumbs.db
*.log

# local design tooling, not the product — the design record lives in PRODUCT.md
.claude/
.impeccable/
